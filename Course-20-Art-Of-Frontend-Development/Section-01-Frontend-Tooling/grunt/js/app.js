(function () {
    // Message to display
    var hereIGo = 'Houston we have liftoff';

    /**
     * Initial startup method.
     * Initializes app and prepares any necessary configuration
     **/
    function bootstrap() {
        console.log(hereIGo);
    }

    bootstrap();
})();